/**
 * Enum que define los posibles estados de una terminal de peaje.
 * Se exporta para ser reutilizado en la Entity, DTOs y Services.
 */
export enum EstadoTerminal {
  ACTIVO = 'Activo',
  INACTIVO = 'Inactivo',
  ROTO = 'Roto',
}
