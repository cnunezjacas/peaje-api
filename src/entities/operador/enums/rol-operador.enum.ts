/**
 * Enum que define los posibles estados de un operador
 * Se exporta para ser reutilizado en la Entity, DTOs y Services.
 */
export enum RolOperador {
  COBRADOR = 'Cobrador',
  SUPERVISOR = 'Supervisor',
  ADMINISTRADOR = 'Administrador',
}
